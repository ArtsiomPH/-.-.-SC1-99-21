# рубли и копейки


m = int(input('Введите количество рублей: '))
n = int(input('Введите количество копеек: '))
s = int(input('Введите количество товара: '))


def price(a, b, c):
    a = m * s + (n * s) // 100
    b = (n * s) % 100
    return a, b, s


M, N, L = price(m, n, s)

# M = m * s + (n * s) // 100
# N = (n * s) % 100
print(" Общая цена составляет", M, "рублей", N, "копеек за", L, "товаров")
